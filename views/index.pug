extends layout.pug

block content
  h1= title
  p Welcome to #{title}
  ul
    each message in messages
      p User: #{message.user}
        div
          blockquote Message: #{message.message}
          blockquote Sent: #{message.sent}
  a(href="/new")="New Message"
